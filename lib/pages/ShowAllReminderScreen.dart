import 'package:flutter/material.dart';

class ShowAllReminderScreen extends StatelessWidget {
  const ShowAllReminderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text("ShowAllReminderScreen"),
      ),
    );
  }
}
